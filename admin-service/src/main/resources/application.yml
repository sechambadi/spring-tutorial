server:
  port: 9761

management:
  security:
    enabled: false
  endpoint:
    health:
      show-detail: always
  metrics:
    tags:
      application: admin-service
    export:
      prometheus:
        enabled: true
  endpoints:
    web:
      exposure:
        include: "*"


